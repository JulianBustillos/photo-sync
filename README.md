# PhotoSync

Photosync purpose is to provide an UI to synchronise media files (recursively) from one path to another.
Imported files are sorted in folders using year, month, day. Already existing files are not copied (based on file content, not file name).
Files can also be removed after copy.
